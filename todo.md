# 프로젝트 목적 달성을 위한 TODO

## 현재 상태 (2026-09-25)

아래 원래 분석은 구현 전 기록이다. 현재 구현과 검증 상태는 다음과 같다.

| 범위 | 구현 및 검증 | 남은 승인 조건 |
|---|---|---|
| P0 결과 보존 | 파일명+경로 해시, 임시 출력, 교체 실패 복원, 실패 기록 테스트 | 강제 종료·동시 쓰기에서 단일 원자 교체는 보장하지 않음 |
| P0 좌표 | 회전·CropBox·업스케일 16조합, 픽셀 및 역변환 검사 통과 | 실제 문서 정답 대조 |
| P1 혼합 텍스트 | 네이티브+OCR 병합, 중복 제거, 단어별 출처 | 복잡한 OCR 분할 및 실제 혼합 PDF 평가 |
| P1 치수 | 의미 유형과 공간 소속 분리, 표 내부 원래 유형 보존 | 일반 숫자와 실제 치수의 정답 라벨 승인 |
| P1 전처리 | 선택적 회전 인식·타일 OCR, 지원 범위 README 명시 | deskew·세로 읽기 순서 미지원, 도전 문서 평가 |
| P1 표 | 비직사각형 병합 분리·불확실성 표시, 144개 경계 조합 단일 점유 검사 | 실제 끊긴 괘선 문서 평가 |
| P1 마스킹 | 단어별 마스크 및 벡터화 경로 수정, 단어 사이 선 보존 | 문자와 선이 겹치는 사례의 연결·폐곡선 평가 |
| P1 네이티브 벡터 | 클리핑·중복 제거·연결 그룹·곡선 오차 제한·coverage fallback | 실제 PDF 품질 및 비용 측정 |
| P1 회귀 평가 | 모델 없는 회귀 테스트, 평가 CLI, CI 정의 추가 | 기존 샘플 정답 라벨·held-out 점수·품질 목표 확정 |
| P2 설정·진단 | 설정 오류 조기 차단, `--check`, CPU fallback | 환경별 GPU·모델 조합 통합 검사 |
| P2 VLM | 문서 호출·시간 예산, 연속 실패 차단, 공통 축소·timeout, 미측정 점수 표시 | 실제 provider 통합 검사, SDK timeout은 강제 프로세스 제한이 아님 |
| P2 결과 계약 | JSON 스키마, 좌표·셀·벡터·참조 검증, manifest, 뷰어 실패 표시 | 원격 CI 실행 및 운영 환경 검증 |

검증 명령: `python -m unittest discover -s tests -v`.
현재 회귀 검사 27개 통과. 모델 다운로드 없이 실제 CPU EasyOCR smoke test도 통과했다.
임시 PDF 2페이지의 실제 로더→벡터→manifest 저장 통합 검사를 통과했다.
브라우저에서 기존 결과 4개, 실패 fixture 및 미측정 점수 표시를 확인했다.
기존 입력·출력은 재처리하지 않았다. 외부 VLM 호출이나 실제 문서 인식률 측정은 수행하지 않았다.
실제 정답 데이터가 필요한 항목은 완료로 간주하지 않는다. Windows 교체·지원 범위·평가 방법은 [README.md](README.md)를 따른다.

## 원래 분석 및 승인 기준

분석 기준: 2026-09-25. [README.md](README.md)의 목표인 문서·도면의 페이지 분리, 6종 영역 분류, 표 구조·셀 텍스트 추출, 픽셀 좌표 기반 벡터화를 기준으로 한다. 아래 항목은 구현 변경이 아닌 개선 작업 목록이다.

우선순위: P0 = 결과 유실·핵심 좌표 오류, P1 = 추출 정확도·품질 검증, P2 = 운영·사용성 개선. 완료 조건은 앞으로 검증할 기준이며 현재 통과했다는 뜻이 아니다.

## 분석 요약

- 기본 파이프라인은 이미 구현되어 있다. 페이지 분리, OCR/네이티브 텍스트 선택, 6종 영역 분류, 괘선 표 분석, 도면 중심선 벡터화, JSON/SVG 저장 및 결과 뷰어가 연결되어 있다.
- 목적 달성의 주요 부족점은 기능 개수보다 **좌표 정합성, 정보 누락 방지, 표 구조의 유효성, 추출 품질을 입증할 회귀 검증**이다.
- 기존 출력 요약에는 입력 4개, 총 13페이지·435개 영역이 있다. 이는 실행 산출물의 존재만 보여 주며 정확도나 최신 코드와의 일치를 보증하지 않는다.
- VS Code가 선택한 `venv_lmm` 환경에서 소규모 재현 검사를 실행했다. 기존 입력·출력은 수정하지 않았으며, PDF 검사는 임시 파일로 수행했다.
- 실제 EasyOCR 모델 추론, Ollama/상용 VLM 호출, 전체 입력 재처리, 브라우저 상호작용 검사는 수행하지 않았다. 정답 라벨이 없으므로 실제 문서의 인식률도 산정하지 않았다.

## P0: 결과 보존

- [ ] **입력 파일별 출력 경로 충돌 방지 및 원자적 결과 교체**
  - 근거: [main.py](main.py)의 `process_file()`은 `file_path.stem`으로 출력 폴더를 정하고, 입력을 읽기 전에 기존 폴더를 `shutil.rmtree()`로 삭제한다. `sheet.jpg`와 `sheet.pdf`는 같은 출력 경로를 사용하며, 재처리 실패 시 이전 정상 결과도 사라진다.
  - 확인: 두 파일에 현재 경로 계산식을 적용하면 모두 `output/sheet`가 된다. 실제 결과 삭제를 동반하는 검사는 하지 않았다.
  - 작업: 확장자 또는 원본 경로 식별자를 포함한 출력 키를 정하고, 임시 디렉터리에서 모든 페이지를 처리한 뒤 정상 완료된 결과만 교체한다. 실패 시 이전 결과를 보존하고 실패 내역을 별도로 남긴다.
  - 완료 조건: 같은 이름의 JPG/PDF를 함께 처리해도 결과가 각각 남으며, 손상된 입력이나 중간 페이지 오류를 주입해도 이전 정상 결과는 유지된다.

## P0: 좌표 정합성

- [ ] **회전 PDF의 렌더 이미지와 네이티브 좌표 통일**
  - 근거: [pipeline/loader.py](pipeline/loader.py)의 `load_pages()`와 `_extract_native_vectors()`는 네이티브 좌표에 DPI 배율만 곱한다. 페이지 회전에 따른 좌표 변환은 적용하지 않는다.
  - 재현: 300×200pt 페이지에 텍스트와 선을 넣고 90도 회전한 PDF를 72 DPI로 읽으면 이미지는 200×300px이지만 텍스트 bbox의 x 범위는 220.0~256.1, 선의 x 범위는 220~280으로 이미지 밖에 남는다.
  - 작업: 네이티브 단어의 네 모서리와 벡터 점을 렌더 좌표계로 변환한다. 회전·CropBox·렌더 원점·업스케일을 포함한 변환 정보를 출력에 보존한다. CropBox 관련 오류 여부는 별도 검증한다.
  - 완료 조건: 0/90/180/270도, 비기본 CropBox, 업스케일 조합에서 알려진 텍스트·선 좌표가 렌더 이미지와 허용 오차 1px 이내로 맞는다.

## P1: 텍스트와 영역 분류

- [ ] **혼합형 PDF에서 네이티브 텍스트와 OCR 병합**
  - 근거: [pipeline/ocr.py](pipeline/ocr.py)의 `get_text_items()`는 네이티브 단어가 `pdf.min_native_words` 이상이면 페이지 전체 OCR을 생략한다. 디지털 제목과 스캔 도면이 함께 있는 페이지에서는 스캔 부분을 읽을 경로가 없다.
  - 재현: 네이티브 단어 5개를 넣고 OCR 호출을 mock으로 관찰했을 때 호출 횟수는 0이었다. 실제 OCR 인식 성능을 검증한 것은 아니다.
  - 작업: 네이티브 텍스트의 공간 커버리지·품질을 평가하고, 미포함 영역 또는 전체 페이지에 OCR을 보완 적용한다. 좌표·텍스트 기반 중복 제거와 항목별 출처를 유지한다.
  - 완료 조건: 디지털 제목+스캔 본문, 소량 네이티브 텍스트, 중복 OCR 레이어 PDF에서 기대 텍스트가 빠지거나 두 번 출력되지 않는다.

- [ ] **도면 내부 치수의 의미 보존 및 분류 기준 확정**
  - 근거: [pipeline/regions.py](pipeline/regions.py)의 `reclassify_text_regions()`는 기본 설정에서 도면 bbox와 충분히 겹치는 `dimension`까지 `annotation`으로 바꾼다. 이는 README에 기술된 현 정책이지만, 치수를 독립 유형으로 추출하려는 목표와의 정리가 필요하다.
  - 재현: 도면 bbox 안에 넣은 `dimension` 항목이 `annotation`, 출처는 `ocr+in_drawing`으로 바뀌었다.
  - 작업: 텍스트의 의미 유형과 공간 소속을 분리하거나 원래 분류를 보존한다. 숫자·축척·치수·기호를 구별하는 정답 기준을 먼저 정하고, bbox 포함만으로 모든 숫자를 재분류하지 않는다.
  - 완료 조건: 도면 내부의 `1200`, `R25`, 직경 기호, 허용차와 일반 번호·주석을 구별하는 라벨 테스트를 통과하고, 표 내부의 숫자 처리 정책도 명시된다.

- [ ] **기울기·회전 및 작은 글자에 대한 전처리 범위 정의**
  - 근거: [pipeline/loader.py](pipeline/loader.py)의 전처리는 업스케일 중심이고, [pipeline/ocr.py](pipeline/ocr.py)의 줄 병합은 수평 행을 가정한다. [pipeline/table.py](pipeline/table.py)의 괘선 검출 역시 수평·수직 커널을 사용한다.
  - 작업: 실제 대상 문서에서 필요한 방향 보정·deskew·타일 OCR 범위를 정하고, 지원하는 변환은 원래 페이지로 역변환할 수 있게 한다. 무괘선 표·빈 양식·단일 행/열 표는 현재 지원 범위와 분리하여 요구 여부를 명시한다.
  - 완료 조건: 세로 치수, 90도 스캔, 기울어진 표, 큰 페이지의 작은 글자를 포함한 평가 세트와 지원/미지원 문서 조건이 마련된다. 정확도 개선 여부는 동일 평가 세트에서 측정한다.

## P1: 표와 벡터 정확도

- [ ] **병합 셀의 직사각형 유효성과 중복 방지**
  - 근거: [pipeline/table.py](pipeline/table.py)의 `_merge_spans()`는 누락된 경계로 연결된 셀을 묶은 뒤 직사각형 외접 범위를 반환한다. 연결 집합이 L자형이어도 외접 범위 내부의 다른 셀을 포함한다.
  - 재현: 2×2 표에 `merge_right={(0, 0)}`, `merge_down={(0, 0)}`을 주면 2×2 병합 셀과 우하단 1×1 셀이 함께 반환된다. 우하단 격자의 소유 셀 수가 2가 된다.
  - 작업: 병합 집합이 완전한 직사각형인지 검사하고, 비직사각형은 보수적으로 분할하거나 불확실한 결과로 처리한다. 셀 범위·점유·텍스트 배정의 불변조건을 검증한다.
  - 완료 조건: L자형 누락 경계, 복합 병합, 끊긴 괘선에서 모든 기본 격자는 정확히 한 셀에만 속하며, 셀 bbox가 겹쳐 같은 단어가 중복 배정되지 않는다.

- [ ] **줄 전체 마스킹에 의한 도면 선 삭제 방지**
  - 근거: [pipeline/regions.py](pipeline/regions.py)의 `mask_text()`는 병합된 줄 bbox 전체를 지운다. [pipeline/vectorize.py](pipeline/vectorize.py)도 지역 좌표로 바꾸면서 단어 목록을 넘기지 않는다. 단어 사이의 실제 도면 선까지 지워질 수 있다.
  - 재현: 두 단어 사이의 선 21픽셀을 포함하는 줄 bbox를 마스킹하면 선이 0픽셀로 줄었다. 이 검사는 마스킹 단계의 삭제를 확인한 것으로, 전체 벡터 품질 점수는 아니다.
  - 작업: 가능하면 개별 단어 또는 문자 마스크를 사용하고, 패딩이 선·교차점을 과도하게 지우지 않도록 한다. 선 복원은 실제 연결 근거가 있을 때만 적용한다.
  - 완료 조건: 떨어진 단어 사이의 선은 보존되고 글자는 벡터화되지 않는다. 문자 근처의 직선·분기·폐곡선에 대해 연결 성분 수와 끝점 보존을 검사한다.

- [ ] **네이티브 PDF 벡터를 영역별 결과에 활용**
  - 근거: [main.py](main.py)의 `process_page()`는 네이티브 벡터가 있어도 모든 도면 영역에 래스터 벡터화를 수행한다. [pipeline/export.py](pipeline/export.py)는 네이티브 경로를 페이지 단위 별도 파일로 저장하므로, 영역별 벡터를 사용하는 소비자는 원본 정밀도를 자동으로 활용하지 못한다.
  - 작업: 좌표 정합성 수정 후 네이티브 경로를 영역에 연결하고, 유효한 네이티브 경로 우선·래스터 fallback 정책을 정한다. 표 괘선·문자 외곽선 제외, 경계에서의 클리핑, 중복 제거, 출처 및 연결 그룹 표현을 포함한다.
  - 완료 조건: 벡터 PDF의 알려진 직선·곡선·폐곡선이 영역별로 중복 없이 연결되고, 스캔 PDF는 기존 래스터 경로로 처리된다. 원본 경로 대비 위치·곡선 근사 오차를 측정한다.

## P1: 품질 검증

- [ ] **정답 데이터와 자동 회귀 테스트 구축**
  - 근거: 현재 확인한 워크스페이스에는 자동 테스트·평가 스크립트·CI 설정이 없다. [viewer.py](viewer.py)는 수동 검토를 돕지만, 샘플 출력과 화면 확인만으로는 임계값 변경의 회귀를 판정할 수 없다.
  - 작업: 위 재현 사례를 작은 단위 테스트로 고정하고, 현재 샘플에 영역 유형·bbox·표 셀·텍스트·벡터 정답을 작성한다. 조정용과 평가용 문서를 분리하고, 외부 모델 없이 실행되는 테스트와 모델을 사용하는 통합 검사를 구분한다.
  - 지표: 유형별 영역 precision/recall 및 IoU, 한국어·영문·숫자 OCR 오류율, 표 셀/병합 구조 정확도, 벡터 위치 오차·연결 성분·폐곡선 보존율, 페이지별 시간·최대 메모리.
  - 완료 조건: `venv_lmm`에서 한 명령으로 회귀 검사를 실행하고 실패 시 비정상 종료한다. 대표 문서의 기준 점수와 승인할 최소 품질을 수치로 정하고 CI에서 핵심 불변조건을 검사한다. 품질 목표 수치는 최초 측정 후 결정한다.

## P2: 운영과 결과 계약

- [ ] **설정 검증 및 실행 전 진단**
  - 근거: [pipeline/config.py](pipeline/config.py)의 `load_config()`는 지정 파일이 없어도 기본값으로 진행하고, 설정 키·형식·범위를 검증하지 않는다. [pipeline/ocr.py](pipeline/ocr.py)의 `run_ocr()`는 항상 3개 항목을 언패킹하므로, 공개된 `paragraph` 옵션과 설치된 EasyOCR의 반환 형식 호환성도 확인해야 한다.
  - 작업: 명시한 설정 파일의 부재, 잘못된 provider, 비정상 DPI·커널·비율을 초기에 명확히 거부한다. GPU·모델·입력 디렉터리 상태를 확인하고 CPU fallback 정책을 문서화한다. `paragraph=true`는 지원을 구현하거나 허용하지 않는다.
  - 완료 조건: 설정 오류가 출력 삭제나 모델 호출보다 먼저 보고된다. 기본 설정과 지원 옵션별 smoke test, CPU 전용 실행 검사가 통과한다. `paragraph` 문제는 실제 EasyOCR 호출 전까지 검증 대상 가설로 취급한다.

- [ ] **VLM 호출 예산·실패 상태·분류 신뢰도 명확화**
  - 근거: [pipeline/vlm.py](pipeline/vlm.py)는 `timeout_sec`를 OpenAI에만 전달한다. Gemini는 `vlm_max_image_side`를 적용하지 않고 원본 crop을 인코딩한다. [main.py](main.py)는 모든 정상 VLM 응답에 `confidence=0.9`를 부여한다. 실패 시 휴리스틱 유지 자체는 이미 구현되어 있다.
  - 작업: provider별 timeout·이미지 크기 제한·문서당 호출 예산을 통일하고, 반복 장애 시 호출을 중단한다. 모델명·실패 사유·fallback 여부를 저장하며, 고정 점수를 검증된 확률처럼 표시하지 않는다. 외부 provider 사용 시 문서 이미지 전송 사실을 명시한다.
  - 완료 조건: 지연·연결 실패·잘못된 응답을 mock한 검사에서 정한 시간/호출 예산 내에 fallback하고, 모든 provider의 전송 이미지가 크기 제한을 지킨다. 신뢰도 값의 의미가 결과 명세에 기술된다.

- [ ] **재현 가능한 출력 스키마와 페이지별 처리 상태**
  - 근거: [pipeline/export.py](pipeline/export.py)와 [main.py](main.py)의 결과에는 스키마 버전, 적용 설정, 입력 해시, 모델/라이브러리 버전, 단계별 경고가 없다. 파일 중간 실패는 traceback으로만 보고되며 정상 완료 후에만 파일 요약이 기록된다.
  - 작업: 결과 manifest에 실행 식별자·입력 식별자·설정·버전·좌표 변환·페이지 성공/실패 상태를 기록하고 JSON 스키마를 정의한다. 연결된 출력 파일의 존재, bbox 범위, 셀 span, 벡터 형식도 검증한다. [viewer.py](viewer.py)에서 실패·불완전 결과를 식별할 수 있게 한다.
  - 완료 조건: 결과만으로 어떤 입력·설정·모델을 사용했는지 추적 가능하고, 필수 필드 누락·잘못된 좌표·누락 파일을 자동 검출한다. 기존 정상 결과 보존과 실패 기록이 서로 충돌하지 않는다.

## 권장 착수 순서

1. 확인된 재현 사례를 회귀 테스트로 고정하면서 출력 보존과 PDF 좌표 오류를 먼저 수정한다.
2. 혼합형 PDF 텍스트 보완, 치수 의미 보존, 셀 병합 유효성, 마스킹 문제를 처리한다.
3. 정답 데이터로 기준 성능을 측정하고 네이티브 벡터 활용·전처리 지원 범위를 확장한다.
4. 설정 검증, VLM 예산, 결과 스키마·실패 상태를 정리한다.

학습형 레이아웃 모델 도입, CAD 의미 객체 복원, 편집형 뷰어는 현재 목표의 필수 조건으로 단정하지 않는다. 위 평가 결과로 기존 접근의 한계를 확인한 뒤 별도 범위로 결정한다.